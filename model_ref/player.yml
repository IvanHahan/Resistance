Player:
  properties:
    id:
      type: integer
    name:
      type: string
    role:
      type: string
      enum: [spy, resistance]
      required: false
    game_id:
      type: array
        items:
          $ref: ./proposal.yml